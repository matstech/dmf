"""
tests/conftest.py
-----------------
Shared pytest fixtures and configuration for the DMF test suite.

Add session-scoped or module-scoped fixtures here as the test suite grows
(e.g. a shared in-memory test backend, a pre-loaded spaCy model, etc.).
"""

# No shared fixtures are required at bootstrap stage.
# This file is kept as the canonical extension point.
